#include "main.h"

/**
 * main - Entry point
 * @argc: argument count
 * @argv: argument vector
 *
 * Return: Always 0, success
 */
int main(int argc, char *argv[])
{
	if (argc == 2)
	{
		if (atoi(argv[1]) < 0)
			printf("0\n");
		else
		{
			if argv
		}	
		return (0);
	}
	else
	{
		printf("Error\n");
		return (1);
	}
}
