#include "main.h"
#include <string.h>

/**
 * _strlen - string length
 * @s: string
 * Return: length of string
 */
int _strlen(char *s)
{
	c = 0;

	while(str[c]!='\0')
	{
	_putchar(str[c]);
	c++;
	}
	return (0);
}
