#include "main.h"

/**
 * print_last_digit - print the last digit in a number
 * Return : n
 */
int print_last_digit(int a)
{
        int n;

        if (a < 10)
	{
    		n = n;
	}
        else if (a > 10 && a < 100)
	{
		n = a % 100;
	}
	else if (a > 100 && a < 1000)
	{	
		n = a % 100;
	}

	return (1);
}
